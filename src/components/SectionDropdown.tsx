/**
 * Legacy Play It Pro section switcher — no longer used.
 * Mark's studio and the student portal use dedicated sidebars.
 */
export default function SectionDropdown(_props: { sidebarOpen: boolean }) {
  return null
}
